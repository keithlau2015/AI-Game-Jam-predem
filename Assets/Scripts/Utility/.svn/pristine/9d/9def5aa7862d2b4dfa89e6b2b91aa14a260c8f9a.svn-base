using DG.Tweening;
using UnityEngine;

[ExecuteInEditMode]
public class GUITweener_Rotation : GUITweenerBase<Vector3>
{
    protected override void Awake()
    {
        base.SetUp(DOTween.To(() => transform.rotation, x => transform.rotation = x, to, duration));
        base.Awake();
    }
}
