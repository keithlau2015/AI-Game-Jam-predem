using DG.Tweening;
using UnityEngine;

[ExecuteInEditMode]
public class GUITweener_Position : GUITweenerBase<Vector3>
{
    protected override void Awake()
    {
        base.SetUp(DOTween.To(() => transform.position, x => transform.position = x, to, duration));
        base.Awake();
    }
}
